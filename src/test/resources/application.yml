spring:
    application:
        name: pinkpony
    datasource:
        driverClassName: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/ubuntu
        username: ubuntu
        password:
    jpa:
       hibernate:
          ddl-auto: create-drop
