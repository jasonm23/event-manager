spring:
    application:
        name: pinkpony
    datasource:
        driverClassName: org.postgresql.Driver
        url: jdbc:postgresql://localhost/pinkponytest
        username: neo_test
        password:
    jpa:
       hibernate:
          ddl-auto: create-drop
