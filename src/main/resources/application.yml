spring:
    application:
        name: pinkpony
    datasource:
        driverClassName: org.postgresql.Driver
        url: jdbc:postgresql://localhost/pinkponydev
        username: neo_test
        password:
    jpa:
       hibernate:
          ddl-auto: update
server:
    port: 8080
